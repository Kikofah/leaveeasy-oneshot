# ผลการทดสอบ LeaveEasy

**เว็บที่ทดสอบ:** https://leaveeasy-witchudakhamsom.web.app/
**เครื่องมือ:** Playwright MCP (เปิดเบราว์เซอร์จริง) ผ่าน sub-agent `leaveeasy-tester`
**วันที่รันชุดล่าสุด:** 2026-09-20 (เวลาไทย)
**ผลรวม: ผ่านครบ 5/5 เทสต์ ไม่มีบั๊กค้าง**

---

## ตารางสรุป

| # | ชื่อเทสต์ | ทดสอบอะไร (อิง US) | เวลาที่รัน | ผล |
|---|---|---|---|---|
| 1 | ยื่นใบลาแล้วเห็นในรายการ | US-01, US-02 | 2026-09-20 13:26:23–13:27:36 | ✅ ผ่าน |
| 2 | กดอนุมัติแล้วสถานะเปลี่ยน | US-04 | 2026-09-20 13:30:10–13:31:20 | ✅ ผ่าน |
| 3 | Validation หัวข้อว่าง (เทสต์เสริมที่ผู้ใช้กำหนดเอง) | — | 2026-09-20 13:27:43–13:28:37 | ✅ ผ่าน |
| 4 | ความปลอดภัย — ไม่ล็อกอินเปิดหน้ารายการ/รายละเอียด | US-08 (กฎขั้นต่ำ) | 2026-09-20 13:28:42–13:29:22 | ✅ ผ่าน |
| 5 | ความปลอดภัย — บัญชีที่สองเปิดใบลาของบัญชีแรก | US-08 (rules รายห้อง) | 2026-09-20 13:29:25–13:30:05 | ✅ ผ่าน |

ไม่มีเทสต์ใดไม่ผ่านในรอบนี้

---

## รายละเอียดแต่ละเทสต์

### 1. ยื่นใบลาแล้วเห็นในรายการ (US-01, US-02) — ✅ ผ่าน
สมัครบัญชีใหม่ `leaveeasy.tester.regression1@example.com` สำเร็จ (ไม่มี permission-denied) → ยื่นใบลาครบฟอร์ม กด "บันทึก" → บันทึกสำเร็จ พากลับหน้ารายการอัตโนมัติ เห็นแถวใหม่ครบทุกคอลัมน์ (หัวข้อ / ประเภทการลา / สถานะ=รอพิจารณา / ผู้ขอลา / วันที่ลา) เอกสารจริงในฐานข้อมูล id `3zhKjFazLcJdXWgWRSsF` ไม่มี console error

### 2. กดอนุมัติแล้วสถานะเปลี่ยน (US-04) — ✅ ผ่าน
ล็อกอินด้วยบัญชี manager (`somying@example.com`) เปิดใบลาที่สร้างในเทสต์ 1 (`3zhKjFazLcJdXWgWRSsF`) เห็นปุ่มอนุมัติ/ไม่อนุมัติ กด "อนุมัติ" → สถานะเปลี่ยนเป็น "อนุมัติ" ทันทีบนจอ ปุ่มหายไปและขึ้นข้อความ "ใบนี้พิจารณาแล้ว จึงเปลี่ยนสถานะและลบต่อไม่ได้" เปิดหน้าใหม่ (fresh navigate) ยืนยันสถานะยังเป็น "อนุมัติ" จริงในฐานข้อมูล (ไม่ใช่แค่ในหน่วยความจำ) ไม่มี console error

*หมายเหตุ: ใช้ใบที่สร้างขึ้นเองในเทสต์ 1 แทนใบ seed เพื่อไม่ให้สถานะข้อมูลตัวอย่างเปลี่ยนแปลงเพิ่มเติม*

### 3. Validation หัวข้อว่าง — ✅ ผ่าน
กรอกฟอร์มยื่นใบลาครบทุกช่อง ยกเว้นเว้นช่อง "หัวข้อ" ว่างไว้ กด "บันทึก" → ไม่บันทึกลง Firestore ไม่เปลี่ยนหน้า ขึ้นข้อความเตือน "⚠️ กรอกไม่ครบ — ต้องกรอกทุกช่องก่อนกดบันทึก" ตรวจหน้ารายการยืนยันว่าจำนวนใบลาไม่เพิ่มขึ้น ไม่มี console error

### 4. ความปลอดภัย — ไม่ล็อกอินเปิดหน้ารายการ/รายละเอียด — ✅ ผ่าน
ล็อกเอาต์แล้วเข้า `leave-requests.html` และ `leave-request-detail.html?id=lr001` โดยตรง → เด้งไปหน้า `login.html` ทันทีทั้งสองครั้ง ตรวจ network requests แล้วพบว่า **ไม่มี request ไป Firestore เกิดขึ้นเลยแม้แต่ตัวเดียว** (ปฏิเสธก่อนยิงคำขอ ไม่ใช่แค่ซ่อนผลลัพธ์) ไม่มี console error

### 5. ความปลอดภัย — บัญชีที่สองเปิดใบลาของบัญชีแรก — ✅ ผ่าน
ล็อกอินด้วยบัญชีที่สอง (`leaveeasy.tester.security2@example.com`) แล้วเข้าตรงที่ URL รายละเอียดใบลาของบัญชีแรก (`leaveeasy.tester.retest1@example.com`) → ขึ้นข้อความ **"โหลดข้อมูลจาก Firestore ไม่สำเร็จ: Missing or insufficient permissions."** ซึ่งยืนยันว่า Firestore ปฏิเสธคำขออ่านจริงระดับเซิร์ฟเวอร์ (ไม่ใช่แค่ UI ซ่อนข้อมูลเหมือนก่อนแก้บั๊ก) ไม่มีข้อมูลใบลาหลุดออกมาบนหน้าจอเลย ไม่มี console error

---

## ประวัติที่มาของ 2 เทสต์แรก (บริบทก่อนรอบนี้)

เทสต์ 1 และ 2 เคยรันครั้งแรกก่อนแก้บั๊ก และ**ไม่ผ่าน**ด้วยสาเหตุนี้:
- **เทสต์ 1 (รอบแรก):** สมัครสมาชิกและยื่นใบลาใหม่ล้มเหลวด้วย `Missing or insufficient permissions` เพราะ `firestore.rules` บน production ถูกทับกลับไปเป็นเวอร์ชัน "ห้ามเขียนทั้งฐาน" — แก้โดยเปลี่ยนกฎเป็น `allow read, write: if request.auth != null;` แล้ว deploy ใหม่
- **เทสต์ 2 (รอบแรก):** ทดสอบไม่ได้เลยเพราะไม่มีบัญชี manager/hr ที่ล็อกอินได้จริง — แก้โดยสร้างสคริปต์ `scripts/seed-auth-users.mjs` ผูกบัญชี Auth เข้ากับ uid `u002`/`u003` เดิม

เทสต์ 4 และ 5 พบบั๊กความปลอดภัยจริงในรอบแรก (ข้อมูลใบลาของคนอื่นถูกส่งมาถึงเบราว์เซอร์จริง แม้ UI จะซ่อนไว้ก็ตาม) เพราะกฎขั้นต่ำเช็คแค่ว่าล็อกอินหรือไม่ ไม่เช็คความเป็นเจ้าของเอกสาร — แก้โดยเพิ่มเงื่อนไขระดับเอกสารใน `firestore.rules` เฉพาะ `leaveRequests`/`approvals` ให้อ่านได้เฉพาะเจ้าของหรือ role `manager`/`hr`

ทุกบั๊กที่พบถูกแก้และ deploy ขึ้น production แล้ว รอบทดสอบล่าสุดในตารางด้านบนคือผลหลังแก้ทั้งหมด

---

## ผลข้างเคียงต่อข้อมูลจริง (จากการรันเทสต์รอบนี้)

- บัญชีทดสอบใหม่: `leaveeasy.tester.regression1@example.com` (role employee)
- ใบลาใหม่ id `3zhKjFazLcJdXWgWRSsF` ("ทดสอบ Regression Run - ยื่นใบลาใหม่") ถูกสร้างและอนุมัติแล้วถาวรระหว่างทดสอบ
- ข้อมูล seed เดิม (`lr001`–`lr005`) **ไม่ถูกแตะต้องในรอบทดสอบนี้**

หลักฐานภาพหน้าจอทุกขั้นตอนเก็บไว้ที่ `.playwright-mcp/regression-test*.png`
