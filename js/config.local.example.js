// ─────────────────────────────────────────────────────────────
// js/config.local.example.js — ต้นแบบไฟล์เก็บคีย์ลับ (API key)
//
// วิธีใช้:
//   1. คัดลอกไฟล์นี้เป็น js/config.local.js (ชื่อไฟล์ต้องตรงเป๊ะ)
//   2. ใส่ OpenRouter API key ของตัวเองแทนค่าตัวอย่างด้านล่าง
//   3. js/config.local.js จะไม่ถูกส่งขึ้น GitHub เพราะอยู่ใน .gitignore แล้ว
//
// ⚠️ ห้ามใส่คีย์จริงลงไฟล์นี้ (config.local.example.js) เด็ดขาด
//    เพราะไฟล์นี้ถูก commit เข้า git จริง ใส่คีย์จริงได้เฉพาะใน
//    js/config.local.js เท่านั้น
// ─────────────────────────────────────────────────────────────

window.OPENROUTER_API_KEY = "ใส่คีย์ของคุณตรงนี้";
